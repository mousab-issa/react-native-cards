import { StyleSheet, Text, View } from "react-native";
import React from "react";

const FormRenderer = () => {
  return (
    <View>
      <Text>FormRenderer</Text>
    </View>
  );
};

export default FormRenderer;

const styles = StyleSheet.create({});
