import { StyleSheet, Text, View } from "react-native";

import React from "react";

const EmptyCardsState = () => {
  return (
    <View>
      <Text>EmptyCardsState</Text>
    </View>
  );
};

export default EmptyCardsState;

const styles = StyleSheet.create({});
